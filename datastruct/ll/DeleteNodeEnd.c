#include<stdio.h>
#include "ll.h"
#include "llop.h"

//length -n + 1 node from the begining 
void DeleteNodeFromEnd( Node *list , int n)
{
  int index ;
  index = LengthOfLinkedList(list) - n + 1;
  //so we need to travel until index -1 node
  //then remove the index node


}
