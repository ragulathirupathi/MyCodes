#ifndef _LIOP_H
#define _LLOP_H

//Deletes the n th node from the end, where is "n" is the number of
//the node which is from the end
void DeleteNodeFromEnd(Node *list, int n);

void ReverseLinkedList(Node **list);

#endif
